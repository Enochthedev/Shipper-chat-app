# Database
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# NextAuth.js
# URL where your app is hosted (use http://localhost:3000 for development)
NEXTAUTH_URL="http://localhost:3000"
# Secret key for NextAuth.js session encryption (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="generate-a-secure-random-string-here"

# Google OAuth (Optional - Get from Google Cloud Console)
# Create OAuth 2.0 credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# WebSocket
# WebSocket server URL (use ws://localhost:3001 for development, wss:// for production)
NEXT_PUBLIC_WS_URL="ws://localhost:3001"
# WebSocket server port (default: 3001)
WS_PORT="3001"

# Production (Optional - for WebSocket server CORS)
# Your production frontend URL (e.g., https://your-app.vercel.app)
PRODUCTION_URL=""

# AI Service (Bonus - Optional)
# Primary: Hugging Face (Free tier available)
# Get your API key from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=""

# Fallback: OpenAI (Paid service)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=""

# Alternative: Anthropic Claude (Paid service)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=""
